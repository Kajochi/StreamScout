declare module '*.svg' {
    const content: any;
    export default content;
  }

/*
TypeScript type definitions:
If you are using TypeScript, you need to make sure that the type definitions for SVG files are configured correctly.
You can do this by adding a declaration.d.ts file in your project and importing the required type definitions there.
*/